// Replace with your DB logic (Mongo, Prisma, Sequelize, etc.)

export const updateStudentPayment = async (studentId, data) => {
  console.log("Updating student:", studentId, data);

  // Example:
  // await Student.updateOne({ _id: studentId }, data);

  return true;
};
